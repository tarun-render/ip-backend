import { OPTIONS_TYPE } from '../utils';
import type { TwilioClient } from '../utils';
export declare class TwilioService {
    private options;
    private readonly twilioSdk;
    constructor(options: typeof OPTIONS_TYPE);
    get client(): TwilioClient;
}
